const $=id=>document.getElementById(id);
const start=$('start'),speed=$('speed'),status=$('status'),progress=$('progress'),ping=$('ping'),download=$('download'),upload=$('upload'),network=$('network'),gauge=document.querySelector('.gauge'),chart=$('speedChart');
const DOWN='https://speed.cloudflare.com/__down?bytes=';const UP='https://speed.cloudflare.com/__up?cache=';
let measurements=[],phase='';
function setSpeed(v,record=true){const n=Math.max(0,Number(v)||0);speed.textContent=n.toFixed(n<10?1:0);gauge.style.setProperty('--deg',`${Math.min(360,n/1000*360)}deg`);gauge.setAttribute('aria-valuenow',String(Math.round(n)));if(record&&phase&&n>0){measurements.push({t:performance.now(),speed:n,phase});drawChart()}}
function rand(){return Math.random().toString(36).slice(2)}
async function latency(){const s=[];for(let i=0;i<5;i++){const t=performance.now();try{const r=await fetch(DOWN+'1&cache='+rand(),{cache:'no-store'});if(r.ok)s.push(performance.now()-t)}catch{}}if(!s.length)return null;s.sort((a,b)=>a-b);return Math.round(s[Math.floor(s.length/2)])}
async function downloadTest(){const size=10000000,workers=3,duration=6500,started=performance.now();let total=0;const run=async()=>{while(performance.now()-started<duration){try{const r=await fetch(DOWN+size+'&cache='+rand(),{cache:'no-store'});const b=await r.arrayBuffer();total+=b.byteLength;const elapsed=(performance.now()-started)/1000;const aggregate=total*8/elapsed/1e6;if(aggregate>0)setSpeed(aggregate)}catch{break}}};await Promise.all(Array.from({length:workers},run));const elapsed=(performance.now()-started)/1000;return total?total*8/elapsed/1e6:null}
async function uploadTest(){const size=2000000,payload=new Uint8Array(size),workers=2,duration=5500,started=performance.now();let total=0;const run=async()=>{while(performance.now()-started<duration){try{const r=await fetch(UP+rand(),{method:'POST',body:payload,cache:'no-store',keepalive:false});if(r.ok){total+=size;const elapsed=(performance.now()-started)/1000;const aggregate=total*8/elapsed/1e6;if(aggregate>0)setSpeed(aggregate)}}catch{break}}};await Promise.all(Array.from({length:workers},run));const elapsed=(performance.now()-started)/1000;return total?total*8/elapsed/1e6:null}
async function networkIdentity(){try{const r=await fetch('https://ipwho.is/?output=json',{cache:'no-store'});if(!r.ok)throw Error();const d=await r.json();if(!d.success)throw Error();const ip=d.ip||'';$('ip').textContent=ip||'Unavailable';$('ipVersion').textContent=d.type||(ip.includes(':')?'IPv6':'IPv4');$('isp').textContent=d.connection?.isp?`${d.connection.isp}${d.connection.asn?' · AS'+d.connection.asn:''}`:'Unavailable';$('location').textContent=[d.city,d.region,d.country].filter(Boolean).join(', ')||'Unavailable'}catch{$('ip').textContent='Unavailable';$('ipVersion').textContent='Unavailable';$('isp').textContent='Unavailable';$('location').textContent='Unavailable'}}
function browserName(){const ua=navigator.userAgent;if(/Edg\//.test(ua))return'Microsoft Edge';if(/Chrome\//.test(ua)&&!/Edg\//.test(ua))return'Google Chrome';if(/Firefox\//.test(ua))return'Mozilla Firefox';if(/Safari\//.test(ua)&&!/Chrome\//.test(ua))return'Safari';return'Unknown'}
function deviceName(){const ua=navigator.userAgent;if(/Android/i.test(ua))return'Android';if(/iPhone|iPad|iPod/i.test(ua))return'iOS';if(/Windows/i.test(ua))return'Windows';if(/Mac OS/i.test(ua))return'macOS';if(/Linux/i.test(ua))return'Linux';return'Unknown'}
function transport(){try{const entries=performance.getEntriesByType('resource');const e=entries.find(x=>x.name.startsWith('https://speed.cloudflare.com'));return e?.nextHopProtocol||'Not exposed'}catch{return'Not exposed'}}
function drawChart(){if(!chart)return;const dpr=window.devicePixelRatio||1,w=chart.clientWidth,h=chart.clientHeight;chart.width=w*dpr;chart.height=h*dpr;const ctx=chart.getContext('2d');ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,w,h);if(!measurements.length){ctx.font='12px system-ui';ctx.fillStyle=getComputedStyle(document.body).color;ctx.globalAlpha=.45;ctx.fillText('Run a test to see live speed measurements',20,h/2);return}const pad={l:42,r:16,t:18,b:32},cw=w-pad.l-pad.r,ch=h-pad.t-pad.b,max=Math.max(10,...measurements.map(x=>x.speed))*1.12,minT=measurements[0].t,maxT=measurements[measurements.length-1].t||minT+1,x=t=>pad.l+(t-minT)/Math.max(1,maxT-minT)*cw,y=v=>pad.t+ch-(v/max)*ch,fg=getComputedStyle(document.body).color;ctx.font='10px system-ui';ctx.fillStyle=fg;ctx.strokeStyle=fg;ctx.lineWidth=1;ctx.globalAlpha=.13;for(let i=0;i<=4;i++){const yy=pad.t+ch*i/4;ctx.beginPath();ctx.moveTo(pad.l,yy);ctx.lineTo(w-pad.r,yy);ctx.stroke();ctx.globalAlpha=.55;ctx.fillText(`${Math.round(max*(1-i/4))}`,5,yy+3);ctx.globalAlpha=.13}ctx.globalAlpha=1;['download','upload'].forEach(p=>{const a=measurements.filter(m=>m.phase===p);if(!a.length)return;ctx.beginPath();a.forEach((m,i)=>i?ctx.lineTo(x(m.t),y(m.speed)):ctx.moveTo(x(m.t),y(m.speed)));ctx.lineWidth=2.5;ctx.lineJoin='round';ctx.lineCap='round';ctx.strokeStyle=p==='download'?'#647ee8':'#9b72d8';ctx.shadowBlur=7;ctx.shadowColor=ctx.strokeStyle;ctx.stroke();ctx.shadowBlur=0});ctx.font='11px system-ui';ctx.globalAlpha=.9;ctx.strokeStyle='#647ee8';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(pad.l,h-10);ctx.lineTo(pad.l+16,h-10);ctx.stroke();ctx.fillStyle=fg;ctx.fillText('Download',pad.l+22,h-7);ctx.strokeStyle='#9b72d8';ctx.beginPath();ctx.moveTo(pad.l+102,h-10);ctx.lineTo(pad.l+118,h-10);ctx.stroke();ctx.fillText('Upload',pad.l+124,h-7);ctx.globalAlpha=1}
start.onclick=async()=>{if(!navigator.onLine){status.textContent='No internet connection';return}start.disabled=true;measurements=[];drawChart();ping.textContent='…';download.textContent='…';upload.textContent='…';progress.style.width='3%';setSpeed(0,false);status.textContent='Finding latency…';const p=await latency();ping.textContent=p??'—';progress.style.width='18%';phase='download';status.textContent='Measuring download…';const d=await downloadTest();download.textContent=d?d.toFixed(1):'—';setSpeed(d||0,true);progress.style.width='62%';phase='upload';status.textContent='Measuring upload…';const u=await uploadTest();upload.textContent=u?u.toFixed(1):'—';setSpeed(u||0,true);progress.style.width='100%';phase='';setSpeed(d||u||0,false);status.textContent='Test complete';detectNetwork();start.disabled=false};
$('theme').onclick=()=>{document.documentElement.classList.toggle('light');drawChart()};
['online','offline'].forEach(ev=>window.addEventListener(ev,detectNetwork));const c=navigator.connection||navigator.mozConnection||navigator.webkitConnection;if(c?.addEventListener)c.addEventListener('change',detectNetwork);window.addEventListener('resize',drawChart);networkIdentity();detectNetwork();drawChart();
